# Copy this file to .env and fill in values for your environments
# These values are read via app.config.ts and available at runtime via Expo Constants

# Base URL of your API server, e.g. https://api.example.com or http://localhost:3000
API_URL=

# Optional app environment label
APP_ENV=development
